# Weather-Analytics-Pipeline
This project is a production-style ETL and ELT pipeline that extracts weather data from the Open-Meteo API (https://open-meteo.com/), transforms and validates it, loads it into a star-schema SQL database, and runs automatically every day via Apache Airflow.
